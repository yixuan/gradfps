PKG_CPPFLAGS = -I./include
PKG_LIBS = -L./primme -lprimme $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)


.PHONY: all clean

all: $(SHLIB)

$(SHLIB): primme/libprimme.a

primme/libprimme.a:
	cd primme && $(MAKE) all CC="$(CC)" AR="$(AR)" CFLAGS="$(ALL_CFLAGS)"

clean:
	$(RM) *.o
	$(RM) *.dll
	$(RM) *.so
	$(RM) *.dylib
	cd primme && $(MAKE) clean
